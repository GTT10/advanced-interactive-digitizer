# HSV color-marker pipeline dependencies.
# Core detection + CLI + browser reviewer (serve.py uses only the stdlib):
numpy>=1.24
opencv-python-headless>=4.8
PyYAML>=6.0
# matplotlib reviewer (review_points.py) and the demo generator (tools/make_demo.py):
matplotlib>=3.7
# Optional, only for the end-to-end browser test harness:
#   playwright>=1.40   (then: python -m playwright install chromium)
